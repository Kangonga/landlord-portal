import { styled } from "@mui/material";

export const HomeContainer = styled("div")(() => ({
  display: "flex",
  flexDirection: "column",
  gap: "1rem",
  padding: ".75rem",
  minHeight: "100dvh",
}));

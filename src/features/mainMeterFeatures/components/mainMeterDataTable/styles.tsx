import { styled } from "@mui/material";

export const MeterContainer = styled("div")(({}) => ({
  display: "flex",
  flexDirection: "column",
  flexWrap: "wrap",
  gap: "1rem",
  padding: ".75rem",
}));
